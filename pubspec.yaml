name: tone_curve
description: Components that can be used with Flutter to add tone curve functionality.
version: 0.0.2
repository: https://github.com/nat-s3/tone_curve
issue_tracker: https://github.com/nat-s3/tone_curve/issues
publish_to: 'none'

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: '>=3.29.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter: